#include <stdio.h>
#include <stdio.h>

/**
 * positive_or_negative - Checks if a number is positive or negative.
 * @i: The number to check.
 */

void positive_or_negative(int i)
{
	/* check for i */
	if (i > 0)
		printf("%d is positive\n", i);
	else if (i == 0)
		printf("%d is zero\n", i);
	else
		printf("%d is negative\n", i);

	/* The if-else block above determines the number's sign and prints the result. */

	/*
	 * You can add more comments or code here
	* to reach the desired line count of 16.
	*/
}
